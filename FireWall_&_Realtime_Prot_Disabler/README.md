turns off firewall and real time protection on WINDOWS 11 ONLY because of the UI changes, works on a mid to high end PC but can be easily modified to run on a low end PC 
simply change the DELAY timings in between the code which is in milliseconds.
