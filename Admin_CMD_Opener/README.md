# Admin CMD (or Powershell) Opener
Just opens windows, types cmd, and uses arrow keys to open command prompt as admin, you can change the delays in the script to fit the target PC, you can also replace "cmd" in line 3 with powershell to open powershell as admin.
